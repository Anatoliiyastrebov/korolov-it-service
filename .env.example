# Скопируйте этот файл в .env и заполните реальными значениями.
# .env коммитить НЕЛЬЗЯ — он в .gitignore.

# IONOS SMTP — параметры из Webmail IONOS / Cockpit IONOS.
# Документация: https://www.ionos.de/hilfe/e-mail/allgemeine-themen/serverdaten-fuer-die-einrichtung-eines-e-mail-kontos/
SMTP_HOST=smtp.ionos.de
SMTP_PORT=465
SMTP_USER=info@korolov-it-service.de
SMTP_PASS=

# Кому уходят уведомления и от кого. Обычно — то же самое:
MAIL_FROM=info@korolov-it-service.de
MAIL_TO=info@korolov-it-service.de
MAIL_SUBJECT=Neue Anfrage über die Website

# Защита от флуда: сколько отправок разрешено с одного IP в минуту.
CONTACT_RATE_LIMIT_PER_MIN=5

# Диагностика SMTP: при значении 1 ответ /api/contact будет содержать
# поле "debug" с реальным кодом ошибки nodemailer (например EAUTH 535).
# Включайте только временно, при отладке. Никаких паролей не возвращает.
# CONTACT_DEBUG=1

# Отправить клиенту копию-подтверждение на его email (From = MAIL_FROM, To = email из формы).
# Нельзя подставлять email клиента в поле From при SMTP через IONOS — сервер отклонит.
# CONTACT_SEND_CLIENT_COPY=1

# Порт для production-сервера (опционально, IONOS обычно сам выставляет PORT).
PORT=8080
HOST=0.0.0.0
